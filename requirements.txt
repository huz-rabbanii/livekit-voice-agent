# Core — LiveKit Agents with STT/LLM/TTS plugins + VAD
livekit-agents[openai,deepgram,silero]>=0.12.0

# Server-side API: token generation, room management (used by console.py)
livekit-api>=0.7.0

# Environment variable loading
python-dotenv>=1.0.0

# Timezone support (stdlib on Python 3.9+, but tzdata provides the database)
tzdata>=2024.1
